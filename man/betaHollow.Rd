% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Fauna.R
\name{betaHollow}
\alias{betaHollow}
\title{Calculate the angle between the wind direction and hollow axis}
\usage{
betaHollow(
  wind_az_deg,
  wind_elev_deg = 0,
  hollow_az_deg,
  hollow_tilt_deg = 0,
  wind_is_from = TRUE
)
}
\arguments{
\item{wind_az_deg}{Numeric. Wind azimuth in degrees, measured clockwise from
North (0° = North, 90° = East, 180° = South, 270° = West).}

\item{wind_elev_deg}{Numeric. Wind elevation angle in degrees above the horizontal
(positive = upward, negative = downward). Default is 0 (horizontal flow).}

\item{hollow_az_deg}{Numeric. Hollow azimuth in degrees, measured clockwise from
North. Defines the horizontal orientation of the hollow’s outward axis.}

\item{hollow_tilt_deg}{Numeric. Hollow tilt angle in degrees above the horizontal
(positive = tilted upward, negative = downward). Default is 0 (horizontal).}

\item{wind_is_from}{Logical. If \code{TRUE} (default), the \code{wind_az_deg} value is the
direction from which the wind blows (meteorological convention). If \code{FALSE},
it is interpreted as the direction towards which the wind is moving.}
}
\value{
Numeric scalar giving the relative angle between the wind vector and
the hollow axis, in degrees. Values range from 0° (direct inflow) to 180°
(direct outflow).
}
\description{
Computes the relative angle (\code{beta_deg}) between a wind vector and a hollow axis,
accounting for both horizontal (azimuth) and vertical (elevation/tilt) angles
of each. A value of 0° means the wind flows directly \emph{into} the hollow mouth,
while 90° indicates crossflow across the opening.
}
\examples{
# East-facing hollow, horizontal wind from West (into mouth)
betaHollow(wind_az_deg = 270, hollow_az_deg = 90)

# Same hollow, wind from North (crossflow)
betaHollow(wind_az_deg = 0, hollow_az_deg = 90)

# Hollow tilted 30° downward, wind along same direction and tilt
betaHollow(wind_az_deg = 270, wind_elev_deg = -30,
                    hollow_az_deg = 90, hollow_tilt_deg = -30)

}
